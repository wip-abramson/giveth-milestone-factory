{
  "name": "giveth-milestone-factory",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ will/giveth-milestone-factory",
    "create-local": "graph create --node http://localhost:8020/ will/giveth-milestone-factory",
    "remove-local": "graph remove --node http://localhost:8020/ will/giveth-milestone-factory",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 will/giveth-milestone-factory"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.16.2",
    "@graphprotocol/graph-ts": "0.16.0"
  }
}
